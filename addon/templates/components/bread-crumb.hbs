{{#if route.linkable}}
  {{#if queryParams}}
    {{#link-to route.path route.params class=linkClass}}
      {{#if hasBlock}}
        {{yield this route}}
      {{else}}
        {{route.title}}
      {{/if}}
    {{/link-to}}
  {{else}}
    {{#link-to route.path class=linkClass}}
      {{#if hasBlock}}
        {{yield this route}}
      {{else}}
        {{route.title}}
      {{/if}}
    {{/link-to}}
  {{/if}}
{{else}}
  {{#if hasBlock}}
    {{yield this route}}
  {{else}}
    {{route.title}}
  {{/if}}
{{/if}}
